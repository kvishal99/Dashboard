venv/
__pycache__/
*.pyc
.env
*.log

# The dashboard's own SQLite history, its WAL sidecars, and the dated copies
# taken before a deploy. Live data - it belongs on each machine's disk, never
# in the repo.
*.db
ops.db-wal
ops.db-shm
ops.db.backup-*

# Uploaded partner spreadsheets - data, not code, and potentially large.
uploads/
<<<<<<< HEAD

# Generated partner event CSVs. Large (one wcities export is 120 MB), always
# reproducible from the database, and pruned after export_keep_days.
exports/
=======
dashboard.pid
>>>>>>> dab6eb3 (Ignore dashboard PID file)
